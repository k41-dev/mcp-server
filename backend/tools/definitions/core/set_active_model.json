{
  "name": "set_active_model",
  "description": "Setzt das aktuell aktive Modell für den Agenten-Kontext. Erlaubte Werte: 'grok', 'ollama', 'openai', 'anthropic'.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "description": "Das zu setzende Modell (grok, ollama, openai oder anthropic)",
        "enum": ["grok", "ollama", "openai", "anthropic"]
      }
    },
    "required": ["model"]
  },
  "category": "core"
}