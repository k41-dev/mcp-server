{
  "name": "delete_memory",
  "description": "Deletes one or more long-term memories. Either by exact memory_id (recommended) or by fuzzy text search (query).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "memory_id": {
        "type": "integer",
        "description": "Exact ID of the memory to delete (from list_memories output)"
      },
      "query": {
        "type": "string",
        "description": "Alternative: delete all memories whose fact contains this text (case-insensitive)"
      }
    },
    "required": []
  },
  "category": "memory"
}