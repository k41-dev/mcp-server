{
  "name": "get_phase_progress",
  "description": "Gibt gespeicherte Phase-Progress-Einträge der aktuellen Session zurück. Optional mit Projekt-Filter.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project": {
        "type": "string",
        "description": "Optional: Projektname oder Identifier zum Filtern (z.B. 'Test Hiking Bodensee')"
      }
    }
  }
}